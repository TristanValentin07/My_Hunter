/*
** EPITECH PROJECT, 2022
** Untitled (Workspace)
** File description:
** call_clock
*/

#include "my.h"

void call_clock(main_t *main)
{
    clock_parallax(main);
    clock_animation(main);
    clock_movement(main);
}
